[package]
name = "temp_http"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.8"
tokio = { version = "1.50.0", features = ["full"] }
percent-encoding = "2.3.2"

# list local network interfaces so we can display all of our addresses
if-addrs = "0.15.0"
